# 软件安装推荐 — PikaOS / Debian Sid

> ⚠️ 本文档供 AI 参考使用，记录用户软件安装偏好与配置要点。
> 系统版本: PikaOS 4 (Debian Sid) | 桌面环境: COSMIC (Wayland) | 架构: x86_64

---

## 目录

1. [微信 WeChat](#1-微信-wechat)
2. [WPS Office](#2-wps-office)
3. [百度网盘](#3-百度网盘)
4. [网易云音乐](#4-网易云音乐)
5. [Visual Studio Code](#5-visual-studio-code)
6. [Git](#6-git)
7. [附录：汇总安装脚本](#7-附录汇总安装脚本)

---

## 1. 微信 WeChat

- **官网**: https://linux.weixin.qq.com/
- **当前版本**: 4.1.1 (2026-05)
- **已下载**: `~/下载/WeChatLinux_x86_64.deb` (212 MB)

```bash
# 安装
sudo dpkg -i ~/下载/WeChatLinux_x86_64.deb

# 运行（安装后菜单栏出现微信图标，或命令行执行）
wechat

# 卸载
sudo dpkg -r wechat
```

**注意**:
- 官方原生 Linux 版，基于 Qt + GTK，非 Wine。
- 中文输入法在 Wayland 下正常工作（前提 `GTK_IM_MODULE=` 留空）。
- 如需重新下载最新版，访问官网点击 X86 → deb。

---

## 2. WPS Office

- **官网**: https://linux.wps.cn/
- **当前版本**: 12.1.2.25882 (2026-04-13)
- **已下载**: `~/下载/wps-office_12.1.2.25882_amd64.deb` (572 MB)

```bash
# 安装
sudo dpkg -i ~/下载/wps-office_12.1.2.25882_amd64.deb

# 如缺依赖
sudo apt install -f
```

**注意**:
- 安装后首次打开可能提示缺字体。解决方案：
  ```bash
  sudo apt install fonts-wqy-zenhei fonts-wqy-microhei
  ```
- WPS 官方下载链接有防盗链（timestamp + MD5），直接 wget 需要构造签名。官网页面点击"立即下载"弹出选择框，选择 deb 格式。
- Linux 个人版不支持购买会员，会员功能需在 Win/Mac/iOS/Android 端购买后登录使用。

---

## 3. 百度网盘

- **官网**: https://pan.baidu.com/download/
- **推荐版本**: baidunetdisk_4.17.7_amd64.deb

```bash
# 方式一：从教育网镜像下载（推荐，速度快）
wget https://mirrors.sdu.edu.cn/spark-store-repository/amd64-store/network/baidunetdisk/baidunetdisk_4.17.7_amd64.deb

# 方式二：从官网下载（官网有时不稳定）
# 访问 https://pan.baidu.com/download/ 点选 Linux 版本

# 安装
sudo dpkg -i baidunetdisk_4.17.7_amd64.deb
sudo apt install -f
```

**注意**:
- 百度网盘官方 Linux 客户端长期未更新（最新版约 4.17.x），功能有限但基础上传/下载可用。
- 登录后仍有限速（与 Windows 端一致），SVIP 可加速。
- 官方下载链接有时失效，镜像源更稳定。

---

## 4. 网易云音乐

- **官网**: https://music.163.com/#/download
- **官方 Linux deb**: 旧版 v1.2.1 (2019-04)

### 方式一：官方旧版 deb（推荐度：⭐⭐）

```bash
# 下载官方旧版
wget https://d1.music.126.net/dmusic/netease-cloud-music_1.2.1_amd64_ubuntu_20190428.deb

# 安装
sudo dpkg -i netease-cloud-music_1.2.1_amd64_ubuntu_20190428.deb
sudo apt install -f
```

安装后如果启动报错 `undefined symbol: g_module_open_full`，需要打补丁：

```bash
# 修复 libgio 兼容性
sudo cp /usr/lib/x86_64-linux-gnu/libgio-2.0.so.0 /opt/netease/netease-cloud-music/libs/

# 修改启动脚本
sudo sed -i '/^exec/i\cd /lib/x86_64-linux-gnu' /opt/netease/netease-cloud-music/netease-cloud-music.bash
```

### 方式二：第三方 GTK4 客户端（推荐度：⭐⭐⭐⭐）

基于 Rust + GTK4 的第三方客户端，轻量稳定：

```bash
# Flatpak 安装（推荐）
flatpak install flathub com.github.gmg137.NeteaseCloudMusicGtk4

# 或 PPA 安装
sudo add-apt-repository ppa:gmg137/ncm
sudo apt update
sudo apt install netease-cloud-music-gtk
```

**注意**:
- 官方 Linux 版自 2019 年后未更新，功能陈旧。
- 第三方 GTK4 版 `netease-cloud-music-gtk` 由社区维护，持续更新，推荐使用。
- PikaOS 基于 Debian Sid，PPA 可能不兼容。优先推荐 Flatpak 方式。

---

## 5. Visual Studio Code

- **官网**: https://code.visualstudio.com/download

### 方式一：添加微软源（推荐，自动更新）

```bash
# 导入微软 GPG 密钥
wget -qO- https://packages.microsoft.com/keys/microsoft.asc | gpg --dearmor > packages.microsoft.gpg
sudo install -o root -g root -m 644 packages.microsoft.gpg /etc/apt/trusted.gpg.d/
rm packages.microsoft.gpg

# 添加 VS Code 源
sudo sh -c 'echo "deb [arch=amd64] https://packages.microsoft.com/repos/code stable main" > /etc/apt/sources.list.d/vscode.list'

# 安装
sudo apt update
sudo apt install code
```

### 方式二：直接下载 deb

```bash
wget -O code_latest_amd64.deb https://code.visualstudio.com/sha/download?build=stable&os=linux-deb-x64
sudo dpkg -i code_latest_amd64.deb
```

**注意**:
- 在 Wayland + COSMIC 下运行正常。
- 推荐安装以下扩展：中文语言包 (`ms-ceintl.vscode-language-pack-zh-hans`)、Python、ESLint、GitLens 等。

---

## 6. Git

Git 系统源直接安装：

```bash
sudo apt install git git-lfs
```

首次使用配置：

```bash
git config --global user.name "Edouardlicn"
git config --global user.email "your@email.com"
git config --global init.defaultBranch main
git config --global core.editor nano
```

---

## 7. 附录：汇总安装脚本

以下脚本将安装所有推荐软件（不包括已下载的微信和 WPS，需手动安装 deb）：

```bash
#!/bin/bash
set -e

echo "=== 更新源 ==="
sudo apt update

echo "=== 安装 Git ==="
sudo apt install -y git git-lfs

echo "=== 安装 VS Code ==="
if ! command -v code &>/dev/null; then
    wget -qO- https://packages.microsoft.com/keys/microsoft.asc | sudo tee /etc/apt/trusted.gpg.d/microsoft.asc >/dev/null
    echo "deb [arch=amd64] https://packages.microsoft.com/repos/code stable main" | sudo tee /etc/apt/sources.list.d/vscode.list
    sudo apt update
    sudo apt install -y code
fi

echo "=== 安装百度网盘 ==="
wget -c https://mirrors.sdu.edu.cn/spark-store-repository/amd64-store/network/baidunetdisk/baidunetdisk_4.17.7_amd64.deb
sudo dpkg -i baidunetdisk_4.17.7_amd64.deb || sudo apt install -f -y

echo "=== 安装 Flatpak + 网易云音乐 ==="
sudo apt install -y flatpak
flatpak remote-add --if-not-exists flathub https://flathub.org/repo/flathub.flatpakrepo
flatpak install -y flathub com.github.gmg137.NeteaseCloudMusicGtk4 || true

echo "=== 安装 WPS 中文字体 ==="
sudo apt install -y fonts-wqy-zenhei fonts-wqy-microhei

echo ""
echo "=== 安装完成 ==="
echo "以下软件需手动安装 deb 包："
echo "  微信:    sudo dpkg -i ~/下载/WeChatLinux_x86_64.deb"
echo "  WPS:     sudo dpkg -i ~/下载/wps-office_12.1.2.25882_amd64.deb"
```

---

> 最后更新: 2026-05-24 | 作者: Edouardlicn
> 本文档专为 AI 辅助参考使用，记录用户环境中的软件安装偏好与注意事项。
