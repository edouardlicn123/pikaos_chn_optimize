# AI 操作指令

本文件定义 AI 读取和执行本项目优化内容的操作流程。

## 前置操作：获取 sudo 密码

AI 在开始任何修改前执行一次密码请求：

1. 请求用户输入 sudo 密码（在对话中直接输入）
2. AI 将密码保存在**会话上下文**中（仅 AI 记忆，不写入任何文件）
3. 验证密码正确性：`echo "$PW" | sudo -S echo "ok"`
4. 验证失败则重新请求，最多 3 次，超过则退出
5. 后续所有 sudo 命令统一使用 `echo "$PW" | sudo -S <command>` 自动执行，用户无需再反复输入
6. 全部操作完毕后提示用户执行 `sudo -k` 清除系统缓存的凭证

## 操作流程

AI 首先向用户说明两个可选模块：**系统优化**和**软件安装**，建议先优化后装软件，但允许用户按任意顺序执行。

### 模块一：系统优化

AI 按以下顺序展示 8 个优化大类，让用户选择执行范围：

| 编号 | 优化项 | 说明 |
|------|--------|------|
| 1 | 内存与交换优化 | swappiness + zram (zstd, 40%) |
| 2 | 服务精简 | mask Evolution/Geoclue/at-spi 等 |
| 3 | 环境变量清理 | Wayland 下 GTK_IM_MODULE 等清理 |
| 4 | fcitx5 输入法配置 | 双拼/快捷键/自启动 |
| 5 | NVIDIA 配置 | 电源管理/硬件加速 |
| 6 | 终端配置（foot） | 字体/server 模式 |
| 7 | 字体配置（CJK 回退） | fontconfig 优先级 |
| 8 | Shell 配置 | Bash 别名/opencode |

支持三种选择模式：
- **a)** 全部执行（输入 `all`）
- **b)** 范围选择（如 `1-4`）
- **c)** 逗号分隔列表（如 `1,3,5,7`）

### 模块二：可选软件安装

AI 读取 `软件安装推荐.md`，整理为清单逐一询问用户是否安装：

> 微信、WPS Office、百度网盘、网易云音乐、Visual Studio Code、Git

用户确认后执行安装，跳过用户拒绝的项目。

### 逐项确认与执行

两个模块统一遵循以下规则：

1. 对于每一项，AI 展示即将执行的**命令摘要**（非全部命令，概括性描述）
2. 询问用户确认（`y/N`），用户同意才执行
3. 执行前先检查当前状态（如 `systemctl --user status`、`zramctl` 等），避免重复修改
4. 每个 `sudo` 命令通过 `echo "$PW" | sudo -S` 执行
5. 命令失败则记录错误信息，继续后续步骤，不阻塞

### 生成报告

全部模块执行完毕后，在项目根目录生成 `优化报告.md`，包含：

- 执行日期时间
- 环境概要（OS/内核/桌面/GPU）
- ✅ 成功项列表
- ❌ 失败项列表（附错误输出）
- ⏭️ 跳过项（用户未选或拒绝）
- 🔄 需重新登录生效的变更
- 🔄 需重启系统生效的变更

## 文件说明

| 文件 | 角色 | AI 操作方式 |
|------|------|-------------|
| `PikaOS系统优化及中文环境设置.md` | 优化执行依据 | 逐节读取，按命令执行 |
| `软件安装推荐.md` | 软件安装参考 | 整理为清单，逐一询问用户 |
| `github.sh` | 用户手动执行脚本 | **AI 跳过，不处理** |

## 注意事项

- 每个 `sudo` 命令前检查 `PW` 变量是否仍有效
- 涉及 `/etc/` 的修改使用 `sudo -S tee` 而非重定向
- 不要修改已有配置文件的备份（如 `/etc/default/zramswap` 编辑前可先备份）
- 更改后的生效条件：环境变量需重新登录，NVIDIA 模块参数需重启，fontconfig 新应用立即生效
